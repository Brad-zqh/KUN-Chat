[project]
name = "kun-chat"
version = "0.1.0"
description = "KUN Chat fan-made AI character web app with approved-only RAG"
requires-python = ">=3.11,<3.15"
dependencies = [
    "livekit-agents[google]~=1.5",
    "livekit-plugins-cartesia~=1.5",
    "python-dotenv>=1.1.1",
    "httpx>=0.28.0",
]

[tool.uv]
package = false
